import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

// Simpele Vite configuratie voor een Vue 3 project.
// Geen extra plugins, zodat het project beginner-vriendelijk blijft.
export default defineConfig({
  plugins: [vue()]
})
